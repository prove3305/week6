public class ArrayPractice {
    public static void main(String[] args) {
        int[] numbers = {10, 20, 30, 40, 50};
        int max = numbers[0];
        for (int i = 0; i < numbers.length; i++) { //배열은 0부터 부조건
            System.out.println(numbers[i]);
        }
    }
}
